# BroadItem 土法编程审阅问题清单

> 源起：2026-08 C++11 降级与 Optional 迁移收尾后，对全库做"土法编程"（hand-rolled/ad-hoc 实现）扫描。
> 本文跟踪发现的问题，逐条讨论、逐条决策。每条记录**现状**（文件:行号证据）、**影响**（可维护性收益）、**改动面**，讨论后回填结论。
>
> **已完成整改、不在本文范围**：可空值哨兵（已迁 `Optional<T>`）、Qt5/Qt6 版本分支（已收口 `compat/`）、z 值常量（已收口 `src/reactive/ZOrder.h`）、`makeUnique`、错误码表（已结构化）。
>
> **状态（2026-08）**：#1~#6、#8、#9、#10、#13、#14 已解决（#9/#10/#13 合并为设计修正，见 doc/运行期诊断状态设计草案.md），#7 跳过（另有打算），其余待讨论。

## 清单总览

| # | 问题 | 影响 | 改动面 | 状态 | 结论 |
|---|------|------|--------|------|------|
| 1 | 路径语法两套实现（Expression vs PropertyContext walker），索引块三处重复 | 高 | 中 | ✅ 已解决 | 根治：Expression 升级为唯一语法权威（类型化分段 + 语法收紧），walker 全部改消费分段，分歧点以测试钉死 |
| 2 | parseNode 的 dynamic_cast 类型分发链 + createElement if-else 字符串链 | 高 | 中 | ✅ 已解决 | 挂载/校验下沉为 `addParsedChild`/`validateChildren` 虚函数，工厂改静态表（doc/解析器挂载下沉.md），parseNode 110→56 行 |
| 3 | reactive 模块样板密度（观察者三连、手写重入守卫、工厂前导校验重复） | 中 | 小-中 | ✅ 已解决 | 保持独立 C++ API 定位；重入守卫 RAII 化（`ReentrancyGuard`）+ 观察者三连折叠；`invokeMethod("resync")` 为既定手法保留 |
| 4 | 对齐取值 stringly-typed（27 处字符串比较，非法值静默兜底无诊断） | 中 | 中 | ✅ 已解决 | 枚举化 + 解析期校验：`Alignment.h` 四枚举（HAlign/VAlign/MainAlign/CrossAlign）+ 解析助手，非法值报新码 BI-P-024 并落默认值；附带 XSD cell 默认值勘误 |
| 5 | "模板 parse 后不可变"纯注释约定，共享 ElementPtr 可被再 parse | 中 | 中 | ✅ 已解决 | 共享边界 const 化：`ConstElementPtr` 贯穿 Registry/Frame/LayoutEngine（doc/模板只读边界.md），负测试验证编译期闭合 |
| 6 | 跨阶段隐式缓存契约（measure→layout→render 的 Node 字段时序） | 中 | 小 | ✅ 已解决 | Text 假缓存消除（render 现算，对齐 ImageElement）；Grid 真缓存加 measured 标志 + layout 断言，顺手修就地改写导致的不可重入；Frame 已有守卫属误报；lazy<T> 泛型机制记录为将来备选 |
| 7 | QPropertyContext 三重 lazy-init（三条触发路径保一次性初始化，动机无注释） | 中 | 小 | ⏭️ 跳过 | 用户另有打算。已查明真实约束：基类构造期 metaObject 不含派生类 Q_PROPERTY（singleShot 之由）、定时器依赖事件循环（API/eventFilter 同步兜底之由）、纯监听场景靠定时器保底——三重非冗余，待后续处理 |
| 8 | 保留绑定名 `{of, prop, as, content, not}` 散落 5 处硬编码 | 中 | 小 | ✅ 已解决 | 收口为 `BindingName` 常量结构体 + `reservedBindingNames()`（与 `BINDING_NS` 同层，仿 ReactiveProperty 模式），五处硬编码全换 |
| 9 | Diagnostics/LayoutRegistry 全局状态的单线程假设仅注释标明 | 低 | 小 | ✅ 已解决 | 设计修正（与 #10/#13 合并）：作用域栈 thread_local 化、collector/Registry 各一把互斥锁、模板 mutable 清零；详见设计草案文档 |
| 10 | 模板级诊断去重挂在每个 Element 上（仅根使用）+ 土法 key 拼接 | 低 | 小 | ✅ 已解决 | 去重状态改由 Frame 持有（实例级窗口），Element 摘除 `m_reportedDiagnostics`/`markDiagnosticReported`，key 结构化为 `DiagnosticKey` |
| 11 | LayoutRegistry 目录扫描：自增 ID 与外部 ID 混用无碰撞防护 | 低 | 小 | 待讨论 | — |
| 12 | 控制元素不入实例树靠 qFatal 运行期兜底，管线签名收 Element& | 低 | 中 | 待讨论 | — |
| 13 | RuntimeScope 依赖人工安装，直调 LayoutEngine 静默失去去重 | 低 | 小 | ✅ 已解决 | Frame 构造即持有去重状态，安装点收敛为成员传递；直调场景"无 scope 不去重"已文档化（Diagnostics.h + 设计草案 2.5） |
| 14 | `matchesProperty` 前缀匹配逻辑双份（Element.cpp / Binding.cpp） | 低 | 小 | ✅ 已解决 | 随 #1 一并收口：唯一实现迁入 `Expression::pathMatches()`，两处委托 |
| 15 | 测试样板：frameFromXmlString 五处复制 + `<root xmlns:b>` 内联手写 | 中 | 小 | 待讨论 | — |
| 16 | example/badges 内联重写 scene_shot.h 已收口的 --shot 逻辑（收口漏网） | 低 | 小 | 待讨论 | — |
| 17 | 测试 mock 的 Q_PROPERTY 样板（test_property_context 两类 82 行逐字） | 低 | 小 | 待讨论 | — |

---

## #1 路径语法两套实现，索引解析三处逐字重复

- **现状**：
  - 解析期：`src/expression/Expression.cpp:15-93` 游标状态机（校验 + 分段），95 行。
  - 运行期：`src/context/PropertyContext.cpp` 另一套游标逻辑——`walkNested`（6-53）、`setWalkInto`（90-194，105 行超长函数）、`advanceBrackets`（197-235）。
  - `[n]` 索引处理块（`indexOf(']')`→`toInt`→负值/越界报错）约 25 行三处近乎逐字重复（PropertyContext.cpp:97-129 vs 201-231 等）。
  - `MapPropertyContext::resolveFirstThenWalk`（MapPropertyContext.h:106-131）与 `PropertyContext::walkPathFromObject`（PropertyContext.cpp:56-87）前奏同构复制；`setPropertyNested` 在 MapPropertyContext.h:80-101 与 QPropertyContext.cpp:119-154 成对重复。
  - 漂移风险实在：Expression 拒绝的语法（如前导 `[`）walker 侧各自判断，两边可静默分叉。
- **方向**：让"分段序列"成为唯一中间表示——解析一次，walker 消费分段而非字符串游标。
- **安全网**：`test_expression` + `test_property_context` 覆盖充分。

### 结论（2026-08，已实施）

选根治方案（讨论中确认两个分歧今天就存在：`a.` 解析期非法/运行期静默接受；`a[0]b` 解析期合法/运行期必报错）：

- `Expression` 升级为唯一语法权威：产出类型化分段 `PathSegment{key | index}`，语法规则成文于类注释并收紧（`a[0]b`、`a.`、`a[0].` 均判非法）；
- `PropertyContext` 三个 walker 合并为 `walkSegments`/`setWalkSegments`（消费分段），`segmentEnd`/`advanceBrackets`/`setWalkInto`/手写 `[n]` 块全部删除；
- `MapPropertyContext`/`QPropertyContext` 入口统一先过 `Expression`；
- 附带收编 #14：前缀匹配唯一实现迁入 `Expression::pathMatches()`；
- 测试：`test_expression` 迁到类型化分段并新增三个收紧用例，`test_property_context` 新增运行期病态路径回归。双栈 ctest 18/18。

## #2 parseNode 类型分发链 + createElement 字符串链

- **现状**：
  - `src/parser/XmlLayoutParser.cpp:198-260`：6 个 `dynamic_pointer_cast`（column/row/grid/cell/for/if）决定子元素挂载方式，grid/for/if 专属校验堆在 parser；函数 110 行（175-284）。
  - 同文件 :136-166：`createElement` 9 标签 if-else 字符串链 + deprecated 装饰器合并分支。
  - 新增部件需改 4 处（工厂链 + parseNode + XSD + include）。项目已有 `canHaveChildren()`/`materializeChildren()` 虚函数先例，此处却用运行时类型识别。
- **方向**：挂载语义下沉为元素虚函数；工厂改静态 `{tag, factory}` 表。新增部件从"改 4 处"变"改 1 处 + 登记 1 行"。

## #3 reactive 模块样板密度

- **现状**（src 最长两文件：`ReactiveBinding.cpp` 598 行、`AnchorDecorator.cpp` 479 行）：
  - `AnchorDecorator.cpp:126-178`：pos/width/height 三个 `createObserver` 块近乎逐字相同（约 50 行可折叠）；`invokeMethod("resync")` 字符串调用无编译期检查。
  - 三个手写重入守卫无 RAII：`ReactiveBinding::m_evaluating`（461-488）、`FollowBinding::m_updating`（152-165）、`AnchorDecorator::m_inGeometryUpdate`。
  - "pos 属性预检"重复（FollowBinding.cpp:38-46 / ConnectionLine.cpp:45-52）；`connectSourceSignal` 与 `ScenePosTracker::connectSignals` 是同一技巧两变体；destroyed→置空→disable 簿记五处各写一遍；create 工厂前导校验三连四份。
  - 错误报告用裸 qWarning，绕过项目 Diagnostics 体系（已知边界：reactive 未与诊断集成）。
- **前置决策**：reactive 模块"未与 XML 布局集成"的前途先定（转正集成 / 保持独立 / 裁剪），再决定投入多少。

### 结论（2026-08，已实施）

前置决策：**保持独立 C++ API 层定位**。`doc/设计.md` 已有约 300 行正式规格章节，且明确 reactive 不纳入诊断体系、保持 qWarning 现状（设计.md:977）——清单中"裸 qWarning 绕过 Diagnostics"实为既定决策而非问题。转正集成 XML 无需求驱动（与"C++ API 层"定位冲突），裁剪排除（文档化产品功能，有测试有示例）。

有限整改两项（其余重复项性价比低，缓办）：

- **重入守卫 RAII 化**：新增 src 内部头 `src/reactive/ReentrancyGuard.h`（仿 ZOrder.h 内部头模式），`ReactiveBinding::m_evaluating`、`FollowBinding::m_updating`、`AnchorDecorator::m_inGeometryUpdate` 三处手写"置位/复位"簿记改为守卫对象，早退路径不再靠人工复位；重入判定语义（静默跳过 / 判循环禁用 / 断言）因场景各异，保留在调用点。
- **观察者三连折叠**：`AnchorDecorator::create` 的 pos/width/height 三个 `createObserver` 块提取共享 `resyncCallback` + 规格表循环，约 50 行逐字重复折叠。

明确不做：`invokeMethod("resync")` 字符串调用保留——`Qt::QueuedConnection` 延迟调用是避免 evaluate 栈内重入误判的既定手法（doc/设计.md:1354），改直接调用会改变时序语义。

测试：双栈 ctest 18/18。

## #4 对齐取值 stringly-typed

- **现状**：`"center"/"end"/"start"/"stretch"/"baseline"/"right"/"bottom"` 等取值裸 QString 存储 + 字符串比较分发，27 处散于 4 文件（TextElement.cpp 11、LinearLayoutCommon.cpp 10、CellElement.cpp 4、LinearLayoutCommon.h 2）。非法取值静默落 default 分支，无解析期校验。`TextElement::render` 内 284-289 与 310-313 对 `m_hAlign` 同函数重复三分支。
- **方向**：枚举化 + 解析期校验（与 BI-P-015/016 诊断体系气质一致）。

### 结论（2026-08，已实施）

选枚举化根治方案。讨论中确认问题最实的一处：非法 `cross-align` 静默兜底为 start 行为，而 XSD 默认是 stretch——拼写错误直接改变布局且无任何提示。

- **枚举权威**：新增公共头 `include/broaditem/element/Alignment.h`——`HAlign`/`VAlign`/`MainAlign`/`CrossAlign` 四枚举（baseline 并入 CrossAlign，仅 row 合法）+ `hAligned()`/`vAligned()` 几何映射 inline + 解析助手 `parseHAlign()` 等（实现 `src/element/Alignment.cpp`）。
- **解析期校验**：非法取值报新码 **BI-P-024**（Error/Default，属性保持默认值）；column 的 `cross-align="baseline"` 从静默退化改为解析期拒绝（`parseAttributes` 按轴判定）。
- **消费侧枚举化**：TextElement/CellElement/ColumnLayout/RowLayout 成员改枚举；27 处字符串比较全部消除，分发改 switch；`TextElement::render` 三重 h-align 分支收敛为 `toQtAlignment()` + `hAligned()` 各一次。
- **附带勘误**：XSD 记载 cell 的 v-align/h-align 默认为 top/left，与代码实际默认 center/center 不一致——按代码为准修 XSD（改代码默认会破坏存量布局与金图基线）。
- **测试**：`test_diagnostics` 新增 3 用例（逐条上报、column 拒绝 baseline / row 接受、layout 属性非法保持默认），码表 32→33；`test_layout_behavior` 新增非法 cross-align 回退 stretch 行为钉。双栈 ctest 18/18。

## #5 "模板 parse 后不可变"纯注释约定

- **现状**：`Element::parse()` 是 public 虚函数（Element.h:44）；`LayoutRegistry::getLayout` 原样返回共享 `ElementPtr`；`ForElement::setTemplate/setBindProperty/setAsVariable` 均 public（XmlLayoutParser.cpp:265-268 在用）。任何消费端可对共享模板再 parse，破坏多实例共享前提（架构承重墙），零强制。
- **方向**：const 化/类型划分/友元限制之一或组合。

## #6 跨阶段隐式缓存契约

- **现状**：`GridNode::colWidths/rowHeights`（measure 写、layout 读，GridLayout.cpp:87-98,127-135）、`TextNode::contentRect`（layout 写、render 读，TextElement.cpp:225,263）、`Frame::m_boundingRect`。"measure 必须先于 layout"全靠注释，乱序调用读陈旧/零值无声失败。
- **方向**：至少断言化（Q_ASSERT 时序标记）；或讨论是否把缓存字段改为显式参数传递。

### 结论（2026-08，已实施）

策略：**能消除的契约连根消除，消不掉的用断言把违反变成显眼失败，已有守卫的不动**。逐一核实后三处的真实面貌与能力各不相同：

- **Text（假缓存，消除）**：`contentRect(rect, style)` 是纯函数，render 时输入（`node.rect`/`node.style`）均在手边——缓存本就不必要，且是全库唯一这么干的叶子部件（`ImageElement` 同族先例：render 现算）。删 `TextNode::contentRect` 字段，render 现算，layout 覆写退化为只写 `node.rect`。消除后乱序在结构上不可能，无需任何检查。原"读零值无声失败"（null 矩形 → 文本错位/消失且无警告）连根拔除。
- **Grid（真缓存，守卫 + 顺手修）**：列宽/行高是 measure→layout 的算法交接，layout 重测会改变 wrap 文本约束语义（行为漂移），消不掉。`GridNode` 加 `measured` 标志（measure 置位），`layout` 入口 `Q_ASSERT(gridNode.measured)`——把空 vector 越界 UB 变成 debug 下指向明确的断言失败，release 零成本。**顺手修不可重入**：layout 原就地改写缓存分配剩余空间（同一 measure 后二次 layout 会重复膨胀列宽），改为拷局部副本分配，缓存保持 measure 纯输出。
- **Frame（误报，不动）**：`paint`/`toImage` 已有 `m_boundingRect.isEmpty()` 守卫（Frame.cpp:206），本项列单属过虑。

**lazy<T> 泛型机制（讨论后暂不采纳，记录为备选）**：讨论中提出"延迟赋值类型 + 编译期开关（`using lazy<T> = T` 一键切零开销）"。方向认可（意图声明进类型、检查可开关），但当前不建：①适用真身仅 Grid 一处，为单点造泛型模板 + 双语义测试矩阵不划算；②Grid 主故障模式是空 vector 越界，发生在 vector 内部，lazy 包装挡不住，其实际保护等价于 bool 标志 + 断言；③Text 场景正确解是删缓存而非给缓存套仪器。若将来跨阶段缓存字段增多（出现第三、四个实例），有真实样本驱动时再设计泛型机制。

测试：行为等价性由全量回归背书（含金图逐像素），Grid 断言为 debug-only 不需新用例。双栈 ctest 18/18。

## #7 QPropertyContext 三重 lazy-init

- **现状**：`QPropertyContext.cpp:14,30,41-47,105`——两个构造函数各 `QTimer::singleShot(0)` 排一次 `ensureConnected()`，`eventFilter` 再手动调一次，叠加 `m_connected` 守卫。三条触发路径保一个一次性初始化；为何需要 singleShot 推迟（而非构造即连）无任何注释，真实约束只能猜。
- **方向**：弄清真实约束（metaObject 构造期不完整？）后补注释或简化。

## #8 保留绑定名散落硬编码

- **现状**：`{of, prop, as, content, not}` 保留名独立硬编码于 5 处：Element.cpp:243-245（reserved 集合）、XmlLayoutParser.cpp:180-181,265-267,274、ForElement.cpp:23-28、IfElement.cpp:33,53、TextElement.cpp:45-60（content 专用通道）。新增保留名需同步 5 文件。
- **方向**：收口为常量集合（参考 ReactiveProperty/盒模型属性名的既有收口模式）。

### 结论（2026-08，已实施）

按既定先例收口，不造新文件：

- **`Element.h` 新增 `BindingName` 结构体**（`Of`/`Prop`/`As`/`Content`/`Not` 五个 static const QString，定义收口 Element.cpp——C++11 无 inline 变量，与 ReactiveProperty 同模式）+ `reservedBindingNames()` 返回 QSet 静态单例；与 `BINDING_NS` 同层同文件，语义权威仍在 `parseBindings`。
- **五处硬编码全换**：`parseBindings` 的 reserved 集合改由 `reservedBindingNames()` 提供；`XmlLayoutParser.cpp`（wrapFor/wrapIf 判定 + 包装读值 7 处）、`ForElement.cpp`（4 处）、`IfElement.cpp`（5 处）、`TextElement.cpp`（b:content 专用通道 4 处）。
- **边界保留**：限定名回退值 `"b:of"`/`"b:prop"`（含前缀的显示名）与字面量通道的 `"content"`（普通 supported 属性，非绑定命名空间伪属性）不动；`<if>` 的 `equals` 是自有属性非保留名，不收；`tag != "for"/"if"` 是元素标签名，与工厂表同族，不属本项。
- **验证**：替换后 grep 确认绑定命名空间裸字符串零残留；行为零变化，双栈 ctest 18/18；CLion `get_file_problems` 改前基线 0 error/0 warning，改后 IDE 快照滞后报陈旧误报（经三构建目录真实编译 0 error/0 warning 证伪——IDE 检查工具的时效局限，记录备查）。

## #9 全局状态单线程假设仅注释标明

- **现状**：`Diagnostics.cpp:113-131` 三处函数内 static（processCollector/sessionStack/runtimeStack）+ `LayoutRegistry::instance()`。单线程假设仅在 .cpp 注释（119、126 行）标明，公共头 API 文档无该约束；多线程使用无守卫。
- **方向**：头文件 API 文档显式声明线程约束；或讨论是否加锁/TLS。

### 结论（2026-08，已实施）

讨论中定位：与 #10/#13 同根（"模板只读 vs 模板级状态"矛盾 + 运行期上下文无一等对象），合并为设计修正（doc/运行期诊断状态设计草案.md）。逐项落地：

- `sessionStack()`/`runtimeStack()` 改 **thread_local**（栈语义跨线程锁修不好——交错压弹逻辑串线，每线程一条才是正解）；
- `processCollector()` 读写加**互斥锁**，锁内拷贝 shared_ptr 快照、锁外调 `report()`（防用户收集器回调重入死锁）；
- `LayoutRegistry` 的 `m_layouts`/`m_nextId` 加互斥锁（锁仅护 ID 分配与插入，解析在锁外），头文件补线程约束文档；
- 模板 mutable 清零后，共享模板树的跨线程消费不再有数据竞争（C1 消除，见 #10）。

## #10 模板级诊断去重的实现方式

- **现状**：`Element.h:132-138,215` `m_reportedDiagnostics`（mutable QSet）挂在每个 Element 上，但注释自认"仅模板根元素的集合被使用"——每个元素白付一个 QSet。去重 key 土法拼接（`codeToString(code) + '|' + bindingPath`，Diagnostics.cpp:185）。
- **方向**：去重状态移到 RuntimeScope/根节点侧；key 拼接结构化。

### 结论（2026-08，已实施）

- **去重状态改由 Frame 持有**（成员 `m_reportedDiagnostics`），`RuntimeScope` 改持状态指针（`QSet<DiagnosticKey>*`）——去重窗口从"模板生命周期"变为"Frame 实例生命周期"（裁决确认：防刷屏目标不变，同模板 N 实例各报一次更利于定位；设计.md 措辞已同步）；
- `Element` 摘除 `m_reportedDiagnostics` 与 `markDiagnosticReported()`（公共 API 移除，0.x 跨 minor 允许），**模板回到完全只读**；
- key 结构化为 `DiagnosticKey{code, bindingPath}` + `qHash`，字符串拼接删除；
- 顺带收编 `ImageElement::m_pixmapCache`/`m_failedPaths` → 进程级路径键缓存 `src/element/image/ImageCache`（含互斥锁）——模板 mutable 一次清零；
- 语义折衷记录在案：曾评估"挪进 RuntimeScope"（窗口缩为单次管线）与"旁置表 + weak_ptr"（保模板级窗口），分别因语义缩水和复杂度否决。

## #11 LayoutRegistry 目录扫描与 ID 分配

- **现状**：`LayoutRegistry.cpp:23-50`：`*.xml` 过滤硬编码、仅文件系统（不扫 qrc）、`m_nextId` 自增与 `registerLayout` 外部 ID 混用，碰撞无防护。

## #12 控制元素 qFatal 兜底

- **现状**：`Element.cpp:45-70` measure/layout/render 基类实现 qFatal；层级已有 ControlElement/RenderableElement 类型划分，但管线入口签名仍收 `Element&`，编译期管不住。

## #13 RuntimeScope 人工安装

- **现状**：模板级去重要求管线经过 Frame（Frame.cpp:151,184）；直接调 LayoutEngine 的下游（测试 helpers 即如此）静默失去去重，无提示。

### 结论（2026-08，已实施）

Frame 构造即持有去重状态成员，安装点收敛为`RuntimeScope(&m_reportedDiagnostics)`的成员传递——正规管线（Frame）不存在"忘记装状态"的路径。直调 LayoutEngine 场景裁决为**接受"无 scope 不去重"并文档化**（Diagnostics.h 的 reportRuntime 注释 + 设计草案 2.5），直调方可自建 `QSet<DiagnosticKey>` 显式安装 scope。不引入 LayoutEngine 相位级自动 scope（每相位独立去重窗口语义混乱）。

测试：`test_diagnostics` 的 `runtimeDedupOncePerTemplate` 更名 `runtimeDedupOncePerFrame`（语义钉），新增 `runtimeDedupPerFrameInstance`（共享模板两 Frame 各报一次，区分实例级 vs 模板级窗口的判别用例）与 `concurrentParseSessionIsolation`（双线程 400 轮并发 parse，路径不串线 + 进程级收集器无丢失）。`CaptureCollector` 顺手加锁支持并发投递。双栈 ctest 18/18。

## #14 matchesProperty 双份

- **现状**：`Element.cpp:117-122` 与 `Binding.cpp:18-27` 逐字重复，后者注释自认"使用与 Element::matchesProperty() 相同的前缀匹配语义"。

## #15 测试样板

- **现状**：
  - "XML 字符串落临时文件再 fromFile"五处同构：test_update_coalescing.cpp:17-23、test_image_element.cpp:33-41（自有 frameFromXml）、test_bound_attributes.cpp:183-186、test_regression.cpp:115、test_diagnostics.cpp:29-33（FrameFixture）。helpers 缺 `frameFromXmlString` 收口。
  - `wrap()` 已收口（binding_helpers.h:21）但 6 处仍内联手写 `<root xmlns:b>`（test_regression.cpp:81,153,215、test_bound_attributes.cpp:180、test_update_coalescing.cpp:20、test_image_element.cpp:38）。

## #16 badges 的 --shot 漏网

- **现状**：`example/badges/main.cpp:44-54` 内联重写 `--shot` 参数解析 + 离屏出图——正是 `example/common/scene_shot.h` 已收口的逻辑（该头注释自述收口的正是此族重复，badges 为漏网）。
- **附**：示例间 XML 加载路径三种拼法并存（basic/main.cpp:14、complex/main.cpp:86、badges/main.cpp:29）。

## #17 测试 mock Q_PROPERTY 样板

- **现状**：`tests/test_property_context.cpp:11-88` 两个 mock 类 82 行逐字式 READ/WRITE/NOTIFY 样板，结构完全相同仅基类不同。可宏/模板收一半。

---

## 参考：整体观察

- src 最长 5 文件：ReactiveBinding.cpp（598）、AnchorDecorator.cpp（479）、Element.cpp（432）、TextElement.cpp（347）、XmlLayoutParser.cpp（286）。
- 超长函数：AnchorDecorator::create（~133 行）、parseNode（110）、setWalkInto（105）、layoutChildren（~102）、TextElement::render（90）。
